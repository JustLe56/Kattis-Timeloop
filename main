import sys
max = int(sys.stdin.readline())
chantAmount = 0

for chant in range(0,max):
  chantAmount = chantAmount +1;
  if chantAmount <= max:
    print (chantAmount,"Abracadabra")
  else:
    break
